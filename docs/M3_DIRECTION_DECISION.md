# M3 direction decision

## Selected path

**PATH A2 — RMST / JOINT FUNCTIONAL THEORY IS THE PRIMARY PAPER**

## Decision

Step 04 did not validate a sufficiently nontrivial audit-width theorem at fixed observed `R`: A2 and A3 can be feasibility/contract restrictions rather than interval-width reductions once `R` and clean `pi` are fixed. The most defensible surviving direction is therefore the joint finite-grid functional problem, especially RMST versus pointwise survival bounds.

The direct RMST LP is sharp for each finite instance and respects joint survival-mass coupling. In the diagnostic scenario, the RMST width was smaller than the sum of independently optimized pointwise widths. This is only `CHECKED`, not a general theorem, but it is a concrete reason to investigate joint functional identification rather than repeat one-bin scaling.

## Why not the other paths

- **A1:** global/groupwise audit formulas remain elementary until a contract-level worst-case theorem is proved.
- **A3:** label misclassification is under-specified and has direct partial-identification literature threats.
- **A4:** the operator modulus is promising but currently a formulation, not a theorem.
- **A5:** structural survival restrictions are premature; unrestricted finite-grid LP coupling has not been exhausted.
- **A6:** major redesign is not warranted because the RMST/joint route remains testable.

## Gate state

- Audit Theory Gate A: `CONDITIONAL`
- RMST Gate M: `CONDITIONAL`
- Novelty Gate N: `CONDITIONAL`
- Sufficiency Gate S: `CONDITIONAL`
- OT policy: `OT_DEFERRED_FROM_PRIMARY_THEORY`
- Synthetic generation: `DEFERRED_PENDING_IDENTIFICATION_THEORY`

## Required future result

A general theorem must show when direct RMST/joint identified-set bounds differ materially from any valid aggregation of pointwise bounds, and must survive the censored partial-identification and robust-survival literature threats.
