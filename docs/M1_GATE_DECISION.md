# M1 Gate Decision

## Decision

**Primary path: PATH B — CONTINUE BUT PIVOT THEORY.**

**Secondary consequence: PATH C — DROP OT FROM PRIMARY THEORY** unless a later source-backed theorem shows that transport geometry changes a survival conclusion under a clearly interpretable assumption.

## Rationale

The exact one-bin contaminated diameter is mathematically sharp under its finite model, but its derivation is an elementary composition of Huber-neighborhood overlap and the censoring-channel TV contraction. The targeted literature contains mature censoring partial-identification, robust Huber/OT, survival DRO, and OT partial-identification lines. The current evidence is therefore insufficient for a broad “censoring plus contamination” novelty claim.

The rare-subgroup calculation is more promising as a precise allocation question: under trusted labels and fixed clean prevalence, the sharp diameter contains `epsilon/[(1-epsilon) pi g_1]` and saturates at one. But this remains a local derived result, not a demonstrated literature gap. The next theory milestone should test whether subgroup-allocation constraints, label contamination, and restricted classes produce a theorem that is both nontrivial and scientifically consequential.

## Gate decisions

- Novelty Gate N: **CONDITIONAL**. A specific subgroup-allocation theorem may remain distinctive, but the current result could be a direct corollary of general Huber geometry. Literature coverage is not yet complete enough for PASS.
- Sufficiency Gate S: **CONDITIONAL**. The saturation/fragility regimes could alter subgroup reporting, but no decision-stability procedure or empirical analyst-facing consequence has been validated.
- OT verdict: **OT_NOT_JUSTIFIED_YET**.

## Stop conditions for continuation

Drop the current central direction if the subgroup result is found to be an immediate known corollary with no survival-specific consequence, if restricted classes only hard-code desired answers, or if no valid finite-sample procedure can expose the identified set without unverifiable assumptions.

## Not done

No neural model, large dataset, synthetic generator, finite-sample confidence procedure, or Step 03 work was performed.
