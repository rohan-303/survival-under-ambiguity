# M6 direction decision

## Selected path

**PATH T6 — AUDIT PIVOT FAILS; END THIS RESEARCH LINE**

## Gate decision

The stratified exchangeable corruption-location model gives valid exact certification, but the entire one-bin survival result reduces to hypergeometric acceptance sampling followed by division by known `g_h`. Global-cap coupling and censoring-weighted allocations were implemented as exact small reference calculations; they did not establish a distinct theorem beyond standard audit/validation design.

No Step 08 continuation is authorized by this milestone. OT remains `OT_DEFERRED_FROM_PRIMARY_THEORY`; synthetic generation remains `SUSPENDED`.
